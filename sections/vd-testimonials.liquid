{% style %}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{% endstyle %}

{%- liquid
  assign jdgm_reviews_count = shop.metafields.judgeme.all_reviews_count | default: section.blocks.size
  assign jdgm_reviews_rating = shop.metafields.judgeme.all_reviews_rating | default: 5
  assign has_judgeme_data = false

  if shop.metafields.judgeme.featured_carousel != blank or shop.metafields.judgeme.all_reviews_count != blank
    assign has_judgeme_data = true
  endif
-%}

<section
  class="section-{{ section.id }}-padding vd-testimonials color-{{ section.settings.color_scheme }}"
  data-vd-testimonials-section
  data-vd-testimonials-limit="{{ section.settings.reviews_limit }}"
>
  <div class="page-width vd-testimonials__header-shell">
    <div class="vd-testimonials__head">
      <div>
        {% if section.settings.eyebrow != blank %}
          <span class="vd-section-kicker">{{ section.settings.eyebrow }}</span>
        {% endif %}

        {% if section.settings.heading != blank %}
          <h2 class="vd-testimonials__title">{{ section.settings.heading }}</h2>
        {% endif %}
      </div>

      {% if section.settings.text != blank %}
        <div class="vd-testimonials__text rte">{{ section.settings.text }}</div>
      {% endif %}
    </div>
  </div>

  <div class="vd-testimonials__stage" data-vd-testimonials-stage>
    <div class="vd-testimonials__viewport" data-vd-testimonials-viewport>
      <ul class="vd-testimonials__cards" data-vd-testimonials-cards role="list">
        {% for block in section.blocks %}
          {% assign rating = block.settings.rating | default: 5 %}
          <li
            class="vd-testimonials__card"
            data-vd-testimonial-card
            role="listitem"
            {% if block.settings.product_link != blank %}
              data-vd-testimonial-link="{{ block.settings.product_link }}"
            {% endif %}
            {{ block.shopify_attributes }}
          >
            <article class="vd-testimonials__card-shell">
              <div class="vd-testimonials__card-top">
                <div class="vd-testimonials__stars" aria-hidden="true">
                  {% for i in (1..5) %}
                    <span class="{% if i <= rating %}is-active{% endif %}">&#9733;</span>
                  {% endfor %}
                </div>

                {% if block.settings.meta != blank %}
                  {% if block.settings.product_link != blank %}
                    <a href="{{ block.settings.product_link }}" class="vd-testimonials__product-link">
                      {{ block.settings.meta }}
                    </a>
                  {% else %}
                    <span class="vd-testimonials__product-link">{{ block.settings.meta }}</span>
                  {% endif %}
                {% endif %}
              </div>

              {% if block.settings.quote != blank %}
                <blockquote class="vd-testimonials__quote">{{ block.settings.quote }}</blockquote>
              {% endif %}

              <div class="vd-testimonials__meta">
                {% if block.settings.author != blank %}
                  <strong>{{ block.settings.author }}</strong>
                {% endif %}

                {% if block.settings.location != blank %}
                  <span>{{ block.settings.location }}</span>
                {% endif %}
              </div>
            </article>
          </li>
        {% endfor %}
      </ul>

      <div class="vd-testimonials__judge-source" data-vd-testimonials-judge-source aria-hidden="true">
        {% if shop.metafields.judgeme.featured_carousel != blank %}
          {{ shop.metafields.judgeme.featured_carousel }}
        {% endif %}
      </div>
    </div>
  </div>

  <div class="page-width vd-testimonials__footer-shell">
    <div class="vd-testimonials__footer">
      <div class="vd-testimonials__trust">
        <span class="vd-testimonials__trust-label">
          {% if has_judgeme_data %}
            Judge.me
          {% else %}
            Apercu
          {% endif %}
        </span>

        <div class="vd-testimonials__trust-copy">
          <strong>{{ jdgm_reviews_rating | round: 1 }}/5</strong>
          <span>
            {{ jdgm_reviews_count }} avis
            {% if has_judgeme_data %}
              verifies
            {% else %}
              en exemple
            {% endif %}
          </span>
        </div>
      </div>

      {% if section.settings.button_label != blank %}
        <a
          href="{{ section.settings.button_link | default: '#' }}"
          class="vd-arrow-cta vd-testimonials__cta"
          data-vd-testimonials-cta
          {% if section.settings.button_link != blank %}
            data-vd-testimonials-static-link="true"
          {% endif %}
        >
          <span class="vd-arrow-cta__label">{{ section.settings.button_label }}</span>
          <svg class="vd-arrow-cta__icon" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 46 16">
            <path d="M8 0 6.545 1.455l5.506 5.506H-30v2.078h42.052l-5.507 5.506L8 16l8-8Z" transform="translate(30)"></path>
          </svg>
        </a>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "VD Testimonials",
  "tag": "section",
  "class": "section section-vd-testimonials",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Sur-titre",
      "default": "Judge.me"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Titre",
      "default": "Avis clients verifies"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Texte",
      "default": "<p>Quelques retours clients sur les produits les plus commandes, avec un rendu plus sobre et plus precis.</p>"
    },
    {
      "type": "select",
      "id": "reviews_limit",
      "label": "Nombre d'avis",
      "default": "6",
      "options": [
        {
          "value": "5",
          "label": "5"
        },
        {
          "value": "6",
          "label": "6"
        }
      ]
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Libelle bouton",
      "default": "Voir tous les avis"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Lien bouton"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Palette",
      "default": "scheme-3"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Marge haute",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Marge basse",
      "default": 32
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Apercu avis",
      "limit": 6,
      "settings": [
        {
          "type": "range",
          "id": "rating",
          "label": "Note",
          "min": 1,
          "max": 5,
          "step": 1,
          "default": 5
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Citation",
          "default": "\"Une longueur, une souplesse et une profondeur rares. On sent tout de suite que la selection est serieuse.\""
        },
        {
          "type": "text",
          "id": "author",
          "label": "Auteur",
          "default": "Jean-Marc L."
        },
        {
          "type": "text",
          "id": "location",
          "label": "Date ou contexte",
          "default": "Chef patissier, Lyon"
        },
        {
          "type": "text",
          "id": "meta",
          "label": "Nom produit",
          "default": "Bourbon Gourmet"
        },
        {
          "type": "url",
          "id": "product_link",
          "label": "Lien produit"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "VD Testimonials",
      "blocks": [
        {
          "type": "testimonial"
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "\"Les gousses sont charnues, souples, brillantes. Meme avant l'infusion, on voit la difference.\"",
            "author": "Elise B.",
            "location": "Passionnee de gastronomie",
            "meta": "Gousses noires"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "quote": "\"Le produit suit le discours. C'est net, precis, sans promesse en trop.\"",
            "author": "Thomas V.",
            "location": "Collectionneur d'epices",
            "meta": "Extrait premium"
          }
        }
      ]
    }
  ]
}
{% endschema %}
